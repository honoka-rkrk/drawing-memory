import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    neutral: {
      main: "#64748B",
      second: "#fff",
    },
    color2: {
      main: "#500",
      second: "#255",
    },
  },
});

export default theme;
